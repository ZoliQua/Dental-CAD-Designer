DATABASE_URL="file:./dev.db"

# Phase 8 Task 6 — local single-user auth (ADR-020). OPTIONAL: set an explicit
# capability token for the server's mutating-route gate (deployment / a
# multi-machine setup). When UNSET, a fresh local install auto-provisions a
# random token at apps/server/data/auth-token (git-ignored, 0600) — the
# frictionless default. NEVER commit a real token value.
# DQCAD_AUTH_TOKEN=""
