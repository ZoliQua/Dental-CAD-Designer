// packages/cad-pipeline/src/stages — the 6 fixed-order crown-design stages
// (inner surface, anatomy placement, adaptation/morphing, shell
// construction, freeform, QC — docs/plans/phase-4-crown-design.md). Empty
// placeholder: Phase 4 Task 1 is scaffold-only (YAGNI guardrail: "no
// pipeline STAGES yet") — the first real stage (`innerSurface.ts`) arrives
// Task 3.
export {};
