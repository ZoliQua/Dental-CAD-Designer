// packages/clinical-profiles — Versioned, schema-checked material profiles (JSON) holding
// clinical defaults (gaps, thicknesses, connector areas). No clinical defaults live outside
// this package. Populated starting Phase 4.
export {};
