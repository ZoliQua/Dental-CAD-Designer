// packages/kernel/src/blockout — undercut blockout PREVIEW (Phase 3 Task
// 10, "virtual wax"). See blockoutPreview.ts's module doc for the
// display-only scope boundary, construction, and `@errorBound`.
export {
  blockoutPreview,
  toBlockoutPreviewMesh,
  type BlockoutRegion,
  type BlockoutPreviewMesh,
  type BlockoutPreviewOptions,
  type BlockoutPreviewResult,
} from './blockoutPreview.ts';
