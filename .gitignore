node_modules/
dist/
build/
*.db
.env
.env.*
!.env.example
!**/.env.example
.superpowers/
# Raw scan drops (contain patient-identifying filenames — never commit; anonymized copies go to test-fixtures/)
scans/
.DS_Store
playwright-report/
test-results/
coverage/
# Phase 2 Task 12: client-dom (vitest browser mode) auto-captured
# failure screenshots — regenerated on demand by a failing test, never
# committed (same rationale as playwright-report/test-results above).
__screenshots__/
# Task 11: content-addressed mesh file storage (apps/server/src/mesh-storage.ts)
# — uploaded scan bytes, never committed.
apps/server/data/

# Generated, not committed — regenerated deterministically on demand (see
# scripts/generate-large-fixture.ts). Perf-only fixtures too large for
# normal Git/LFS history (the >100 MB fixture docs/plans/
# phase-1-import-viewer.md's Global Constraints calls out explicitly).
test-fixtures/generated/
